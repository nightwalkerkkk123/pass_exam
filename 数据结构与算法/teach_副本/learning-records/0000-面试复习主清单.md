# 数据结构与算法 · 校招面试复习 · 主清单（Living Checklist）

> 本轮从"期末考试"转向"校招笔试+面试"。主控清单，每讲完/验证一格就打勾 `[x]`；答错回炉并新建 learning-record。
> **铁律**：类比/画面先行 → 关键定义 → 小表 → C 代码（同题附 Python/JS/TS 对照）→ ≥1 道自测题 → 她复述 + 打乱选项验证；一节只装 1 个窄概念，复述通过前不推进。
> **主语言 C**：所有题先在 C 写透；同一题附 C(舒适区) + Python/JS/TS 对照版，借 DSA 顺带拓宽语言。
> **C 的硬现实**：无哈希表/动态数组/优先队列容器，部分题要手写结构体；这正是对照语言的展示入口。

## 考试/面试边界一句话

**校招笔试 + 技术面**，1-2 个月。两种形态都要练：
- **笔试**：常 ACM 模式，自己处理输入输出（C 的 `scanf`/`printf`）、多组数据、注意时空限制；常考经典算法实现。
- **面试**：常核心代码模式（写一个函数），重在**边讲边写、思路清晰、复杂度分析、代码无大 bug**。
重点：**思路表达 + 模式识别 + 复杂度分析 + C 手写无大错**。不追竞赛级（线段树/KMP 不在范围，除非真题出现）。

## 当前进度指针（每次会话更新）

- **起点诊断 + Big O（2026-08-01）**：① 摸底：大 O 把 `for(i)×for(j*=2)` 误判 O(n²)；"有序→二分"选对。② 讲透"数次数法"后验证过关（O(n log n)，见 LR 0001）。③ **循环边界也补完**：曾误以为 `j<8` 在 j=4 停（实际 j=4 仍执行、停在 j=8、共 3 次 = log₂8）；改 `j<=8` 验证选对 4 次——`<` vs `<=` 差一次执行，二分边界同源。**Big O 迭代计数 + 循环边界 + 递推式/空间 全过关**（见 LR 0001）。**递归也过关**：`func(4)=24` 选对、循环 vs 递归空间 O(1)/O(n) 选对。**C 手写也过关**（一眼识破野指针 bug）。**🎉 Phase 0 全部完成**（地基三件套：大O / 递归 / C手写）。**Phase 1 对撞双指针也完成**：反转数组（机制：两端换、`left<right` 边界）+ 有序两数之和（推理：`sum` 太小 `left++`、太大 `right--`），均验证过关。下一步：**快慢指针**（移除元素 / 原地去重），之后滑动窗口。**【中途插入·堆专题】**堆是什么/数组存储/上浮下沉/heapify O(n) 全过关（见 LR 0002）；merge sort 核心规则（先分到底再合、两边须已有序）也过关、复杂度 `O(n log n)` 待补。**遗留顽固点**：堆排序方向（升序→max-heap、min-heap→降序）她连错 3 次，口诀已给但未亲手 trace 验证，下次清醒时补。**【全排列·回溯】**应她要求讲了全排列：决策树 + 回溯（DFS+撤销）+ 代码（path/used）+ 三步万能骨架（做选择→backtrack→撤销）+ 撤销时机 + O(n·n!)——**全部验证过关**（4/4）。子集/组合同骨架待学。**【前缀和】**讲了概念（累计和=里程表、区间和=两前缀之差、O(1) 查询），但**区间和公式 `prefix[r+1]-prefix[l]` 左端点 off-by-one 连错 2 次**（用了 `prefix[l-1]`），改教"推导法"（要剔"下标 l 之前的"=`prefix[l]`，l=0 时减 prefix[0]=0），**未再验证、待巩固**。**【树层序/BFS】**讲透：队列 FIFO→层序、BFS 四步万能骨架（出队→处理→邻居入队）、trace、O(n)/O(w)、C 须手写队列——**验证过关**。Obsidian 笔记 `全排列-回溯法.md` 已建（含今日手撕目标）。
- **历史材料**：`../lessons/0001-0020` 为期末考试版（按《算法图解》+《啊哈！算法》章节序），作**概念背景**，不按其顺序推进。注意"生成 ≠ 学会"，这 20 课一节都未验证过。
- **下一步**：摸底 → 锁定 Phase 0 起点 → 逐格推进。

## 复习路线图（按面试频率分 Phase，非书本顺序）

权重＝校招出现频率。「微课」引用 `../lessons/`；新增面试向微课编号从 0021 起。

### Phase 0 — 地基（不牢其他都悬）
| ✓ | 子主题 | 权重 | 现有微课 | 易错点 |
|---|---|---|---|---|
| [~] | Big O：时间/空间、最坏/平均/最好、均摊 | 必 | 0001 | **迭代计数✅ + 递推式/空间✅**(LR0001)；最坏/平均/最好留到快排、均摊留到动态数组/哈希 |
| [x] | 递归思维 + 调用栈 + 递推关系 | 必 | 0003 | ✅ 基线/递归条件 + 调用栈 + 递推式 T(n)=T(n−1)+O(1)→O(n)、空间 O(n)（vs 循环 O(1)）全部验证过关 |
| [x] | C 指针/结构体/malloc/free 基本功（面试手写命根） | 必 | — | ✅ 一眼识破"未 malloc 就解引用"的野指针 bug；`->` vs `.` 没被带偏。够用进 Phase 1（链表处再压 malloc/free/内存释放细节） |

### Phase 1 — 线性结构 + 高频技巧（出现最多）
| ✓ | 子主题 | 权重 | 现有微课 | 易错点 |
|---|---|---|---|---|
| [~] | 数组：双指针（对撞/快慢） | 极高 | — | **对撞✅**（反转数组 + 有序两数之和：边界 `left<right`、`sum>target→right--` 推理全过关）；快慢待学（移除元素/去重） |
| [ ] | 数组：滑动窗口 | 极高 | — | 何时扩窗/缩窗 |
| [ ] | 哈希表（C 里数组模拟/手写） | 极高 | 0005 | 频率统计；冲突 |
| [ ] | 链表：反转、环检测、找中点/倒数第k | 极高 | 0002/0012 | 头/尾/空边界；快慢指针 |
| [ ] | 字符串（=字符数组）：反转、子串 | 高 | — | C 无 string 类型，用 `char[]`+`strlen` |
| [ ] | 栈：括号匹配、单调栈 | 高 | 0012 | 单调栈何时弹 |
| [ ] | 队列：BFS 用、用栈实现队列 | 中 | 0012 | 循环队列判满判空 |

### Phase 2 — 树（面试高频）
| ✓ | 子主题 | 权重 | 现有微课 | 易错点 |
|---|---|---|---|---|
| [ ] | 二叉树四种遍历（前/中/后/层序） | 极高 | — | 递归写法 + 迭代写法（栈/队列） |
| [ ] | 递归题：最大深度、翻转、对称 | 高 | — | 返回值 vs 侧效 |
| [ ] | BST 性质 + 增删查 + 验证 | 高 | — | 中序是有序的 |
| [ ] | 路径题、LCA 最近公共祖先 | 中 | — | — |

### Phase 3 — 排序 + 二分 + 堆
| ✓ | 子主题 | 权重 | 现有微课 | 易错点 |
|---|---|---|---|---|
| [ ] | 二分查找（边界是难点） | 极高 | 0001 | `left<=right` 还是 `<`；更新 `mid±1` |
| [ ] | 快排（partition）+ 第 K 大 | 高 | 0004 | partition 返回值；最坏 O(n²) |
| [ ] | 归并（分治）+ 归并排序 | 高 | 0004 | 合并两段 |
| [~] | 堆/优先队列（C 手写） | 中 | 0017 | 上浮/下沉/heapify O(n) ✅(LR0002)；**堆排序方向（升序→max-heap）待重验** |

### Phase 4 — 图 + 搜索（笔试常见）
| ✓ | 子主题 | 权重 | 现有微课 | 易错点 |
|---|---|---|---|---|
| [ ] | BFS / DFS（网格 + 图） | 高 | 0006/0014/0015 | visited 防重复；方向数组 |
| [ ] | 并查集 | 中 | 0017 | 路径压缩 + 按秩合并 |
| [ ] | 拓扑排序 | 中 | — | 入度表 + 队列 |

### Phase 5 — 进阶模式（大厂/难题）
| ✓ | 子主题 | 权重 | 现有微课 | 易错点 |
|---|---|---|---|---|
| [~] | 回溯：子集/排列/组合 | 高 | 0020 | **全排列✅**（决策树 + 回溯三步骨架「做选择→backtrack→撤销」+ 撤销时机 + O(n·n!)，4/4 验证过关）；子集/组合同骨架待学 |
| [ ] | 动态规划：一维（爬楼梯/打家劫舍/LIS） | 极高 | 0009 | 状态定义 + 转移 + 初始 |
| [ ] | 动态规划：二维（LCS/编辑距离/背包） | 高 | 0009 | 二维表填法 |
| [ ] | 贪心 | 中 | 0008 | 局部最优→全局最优的成立条件 |

### Phase 6 — 实战
| ✓ | 子主题 | 备注 |
|---|---|---|
| [ ] | ACM 模式输入输出（C: `scanf`/`printf`、多组数据） | 笔试特有，必须单独练 |
| [ ] | 限时模拟（边讲边写） | 把"会"练成"限时快写" |
| [ ] | 错题路由回对应 Phase | 查漏闭环 |

## 时间线与里程碑（2026-08-02 制定）

> 假设每天投入 **3-4 小时、每周 6 天**（≈20-24h/周）。投入更多/更少按比例伸缩。题量指 LeetCode 风格累计练习（早期 Easy 多、后期 Medium 为主）。

| 周 | 内容（Phase） | 累计题量 | 里程碑 |
|---|---|---|---|
| W1 | P0：大O巩固 + 递归 + 调用栈 + C 手写；P1：数组双指针 / 滑动窗口 / 哈希 | ~30 | — |
| W2 | 链表(反转/环/中点) + 栈队列 + 字符串；二叉树遍历 + 递归题 | ~70 | — |
| W3 | 二分边界 + 快排/归并 + 堆/TopK；转 Medium | ~110 | **M1 中小稳过** |
| W4 | BFS/DFS(网格+图) + 并查集 + 拓扑 + 回溯 | ~150 | **M2 大厂尝试** |
| W5 | DP：一维(爬楼梯/打家劫舍/LIS) + 二维(LCS/编辑距离/背包) | ~180 | — |
| W6 | ACM 输入输出 + 限时模拟 + 错题二刷 + 贪心 | ~200 | **M3 大厂 attempt-ready** |

**里程碑含义**：
- **M1（~110 题）**：中小公司 / 一般技术面算法轮基本稳。
- **M2（~150 题）**：大厂简单-中等轮有戏；字节/腾讯中等题能上手。
- **M3（~200 题 + 二刷）**：大厂 attempt-ready。要"稳过" top 大厂算法岗，通常再花 3-4 周二刷高频 + 攻 Hard + 模拟面试。

**诚实评估**：1-2 个月从"基础不熟"到 top 大厂硬标准（~200-300 题、Medium 25min 内 70%+ 解出）偏紧——能到 M1-M2（中小稳、大厂试水）；稳过大厂约需**全职 2.5-3 月 / 半职 4-5 月**。两条路：A 守 1-2 月冲 M2 边面边补；B 延至 ~3 月系统刷到 M3 + 二刷。

## 起点与推进顺序

- 先摸底（2026-08-01）：大 O 分析 + 基本数据结构选择，确认她现在站在哪。
- **Phase 0 不跳**：Big O + 递归 + C 手写基本功，不熟就回炉，不假设会。
- 之后按 Phase 1→6，频率高的先；低频（贪心/拓扑）按时间余量取舍。
- 每格推进：类比 → 定义 → 小表 → C 代码(+对照) → 自测 → **她复述过关才打勾**。

## 已知易错点（继承 + 随讲随记）

- **大 O**：嵌套循环≠一定 O(n²)，看内层增量（如 `j*=2` 是 log n）。
- **二分边界**：`left<=right` vs `left<right`、`mid` 更新 ±1，最易写崩。
- **链表**：头/尾/空三类边界；快慢指针起止位置。
- **C 容器**：无现成哈希表/动态数组，部分题要手写（对照 Python 一行解决，正是拓宽语言入口）。
- **DP**：状态定义错则全错；先想"我在哪、我是谁、我从哪来"。
- **通用**（继承 OS 经验）：抽象先给画面再上术语；出题前她常要先澄清，节奏碎、先安抚再问；反复出现"措辞半句"问题，让她口述判据。
