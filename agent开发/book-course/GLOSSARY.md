# 《深入理解 AI Agent》精读课 · 术语表

这门课的标准语言，全部取自《深入理解 AI Agent：设计原理与工程实践》(李博杰) 原文的定义口径——不是通用词典义。写 lesson、出题、做复盘时，一律以此表为准。术语首次在某章出现即收录于该章名下；跨章复用的术语只在首次出现处列一次。

> 术语约定（贯穿全书）：**思考 = reasoning**（模型展开中间推导、“想”的过程）；**推理 = inference**（模型的前向计算与部署运行）。两者是刻意区分的，勿混用。

## 全局 · 核心公式

**上下文**（Context）：
Agent 的'眼睛',是模型能感知到的一切信息,不仅包含环境信息和对话历史,还包含工具定义等内容;对应 RL 的观察空间(Observation Space)。
_避免_：仅理解为'对话历史'或'prompt'

**工具**（Tools）：
Agent 的'手脚',决定 Agent 能做什么事情(搜索网页、调用 API、操作数据库等);对应 RL 的动作空间(Action Space)。
_避免_：混同为普通函数调用

**提议者-审核者**（proposer-reviewer）：
让 Agent 审阅自己输出的交付件(artifact)并迭代改进的方法,用于解决模型过早认为任务完成的问题。
_避免_：等同于泛泛的'自我反思'

**思考**（reasoning）：
本书统一译法,指模型展开中间推导'想'的过程,包括思维链(Chain-of-Thought)、思考模型、思考 token、思考过程。
_避免_：译作'推理'

**推理**（inference）：
本书统一译法,指模型的前向计算与部署运行,如推理时、推理成本、推理栈、推理时扩展。
_避免_：与 reasoning'思考'混用

**whisper coding**：
口述式协作:向语音 Agent 口述提纲,经'口述—调研—讨论—修改'循环迭代产出内容的工作方式。
_避免_：简单等同于语音输入

**智能体强化学习**（Agentic Reinforcement Learning）：
通过在智能体环境中的强化学习,把工具调用能力训进模型参数,使模型在编程、数学、图形界面操作等领域掌握通用能力。
_避免_：泛指一切 RL

**自举**（bootstrap）：
Agent 通过生成代码实现自我进化,就像一个程序员编写了另一个程序员,新程序员又能继续编写下一个。
_避免_：混同编译器 bootstrap 的狭义含义


## 基础 · 概念地图（第 1 章）

**Harness（工程外壳/马具）**（Harness）：
模型之外的全部基础设施，核心是上下文与工具，外加约束、验证、纠正三层保障机制；把模型的能力引导、约束并放大为可靠的任务执行。
_避免_：不要简单等同于『框架』或『SDK』——它是围绕模型的整套工程外壳，不限于某个具体框架。

**轨迹**（trajectory）：
Agent 执行任务过程中不断积累的消息历史——用户消息、模型回复（思考过程+工具调用）、工具执行结果；对应上下文的动态部分。
_避免_：不要与『上下文』整体混用；上下文=静态前缀+轨迹，轨迹只是其中动态增长的那部分。

**静态前缀**（static prefix）：
每次 LLM 调用都自动拼接在轨迹前、且对话中保持不变的部分，即系统提示词+工具定义。
_避免_：不要把用户消息或工具结果算进静态前缀，它们属于动态轨迹。

**消融实验**（Ablation Study）：
逐一移除系统某个组件、观察系统是否退化，从而判断每个组件贡献的实验方法。
_避免_：不是性能基准测试(benchmark)，重点是『去掉A看还正不正常』的对照排除。

**模型即 Agent**（Model as Agent）：
先进模型通过后训练(尤其RL)把工具调用内化为原生能力，何时调/调哪个/传什么参数都由模型自主决定、无需人工编排层的新范式。
_避免_：不要理解为『框架不再重要』——恰恰相反，模型越强 Harness 越关键。

**零样本泛化 / 少样本适应**（Zero-shot Generalization / Few-shot Adaptation）：
零样本=面对从未见过的任务不给任何示例也能通过组合已有知识处理；少样本=在提示中给两三个示范例子就能掌握新任务模式。
_避免_：少样本不是训练/微调，例子只放在提示里，不改模型参数。

**上下文学习**（In-Context Learning）：
通过注意力机制在上下文中做模式检索式的快速适配，能套用已见过的模式但发现不了全新规律；本质更接近模式匹配而非真正的学习，会话结束即消失。
_避免_：不要当作真正的『学习』或等同后训练；它临时、不改参数，只能套用已有模式。

**防呆**（Poka-yoke）：
源自丰田生产体系的设计思路：从设计上让错误无法发生（如 USB 只能单向插入），应用到工具接口(ACI)设计中主动防止模型误用。
_避免_：不是『错误处理/异常捕获』，而是从接口设计上消除出错可能。


## 支柱一 · 上下文与记忆（第 2–3 章）

**上下文工程**（Context Engineering）：
系统性设计、组织并向模型提供完成任务所需全部背景信息的工程实践，是 Harness'上下文与工具'层的核心实现；既是技术问题更是组织问题。
_避免_：不要等同于'提示工程'或'往 prompt 里塞更多内容'——它是更上位的系统性信息供给设计

**KV Cache**：
模型内部优化：缓存前文 token 在注意力机制中已算出的键值对（Key-Value pairs），下一轮只算新增 token，前提是前缀完全不变；加速单次请求内的 token 生成。
_避免_：不要与 Prompt Cache 混为一谈——KV Cache 在推理引擎内、单次请求内

**Prompt Cache**：
构建在 KV Cache 之上的 API 服务层跨请求缓存：服务商对请求前缀匹配，前缀相同则复用已算好的 KV，缓存读取成本约为首次计算的 1/10 到 1/2。
_避免_：不要以为它自动命中——Anthropic 需显式设 cache_control 断点、有写入加价与 TTL；不要与 KV Cache 混用

**Chat Template**：
把结构化 role-content 消息转换为模型训练时见过的线性 token 流的模板（用 <|im_start|>、<|im_end|> 等特殊标记划分角色与边界），由 API 服务端自动完成。
_避免_：不要理解为开发者需手写的东西；也不要与 system prompt 混淆

**渐进式披露**（Progressive Disclosure）：
Agent Skills 的设计哲学：先给模型看一份 Skill 名称+描述的目录摘要（数百 token），需要时再通过专用工具加载完整 SKILL.md 及细则子文档。
_避免_：不要说成'一次性加载全部知识'或普通的懒加载缓存

**Agent 状态栏**（Agent Status Bar）：
Agent 框架在上下文末尾以 user 角色消息持续注入的结构化状态摘要（工具计数、当前时间、TODO 等），把分散的隐式状态提炼为可直接检索的显式知识。
_避免_：这里的 user 角色只是 API 协议槽位，不等于'来自真实终端用户的输入';不要与系统提示词混淆

**上下文蒸馏**（Context Distillation）：
'提前算好、直接查一眼'这套做法的统一名称——把需要模型现场归纳统计才能得到的结论提前算好放进上下文；Agent 状态栏是它最日常的形态。
_避免_：不要与模型蒸馏（知识蒸馏到权重）混淆——这是推理时的上下文层做法

**上下文腐化**（Context Rot）：
上下文窗口远未装满，但因长上下文导致注意力分散、无关内容占大头，Agent 突然找不到关键信息或反复纠结已解决问题的现象。
_避免_：不要与上下文溢出（窗口用完/装不下）混为一谈——腐化是'装得下却找不到'

**注意力储存池**（Attention Sink）：
序列第一个 token 往往吸收异常高的注意力权重（有时超 70%），作为存放'无处安放'剩余注意力的稳定容器；源于 softmax 要求所有权重之和恰为 1。
_避免_：不要当成模型缺陷——这是系统性数学必然现象

**用户记忆**（User Memory）：
针对单个用户 ID 的个性化持久记忆，跨会话提炼出的稳定信息，会被反复改写、合并、淘汰；本质是投入额外算力构建关于用户的简洁预测模型。
_避免_：不要等同于'对话历史'或'轨迹'——轨迹是单次会话按时间追加不可改的流水账，用户记忆是跨会话提炼的档案

**Advanced JSON Cards**：
最高表达力的记忆存储格式，每张卡片除事实外还含 backstory(获取上下文)、person(主体身份)、relationship(与用户关系)和时间戳，实现同名实体消歧，代表从信息存储到知识管理的范式转变。
_避免_：不要与普通 JSON Cards 混淆——后者只是三层嵌套分类键值对，不含 backstory/person/relationship 消歧字段

**BM25**：
稀疏检索的经典概率排序算法，在 TF-IDF 基础上引入 k1(词频饱和度)和 b(文档长度归一化)两参数，靠倒排索引做精确关键词匹配，至今仍是搜索引擎核心组件。
_避免_：不要说它懂语义——BM25 只按词频和文档频率算分、读不懂同义表达；学习型稀疏(SPLADE/BGE-M3稀疏分支)才引入神经权重

**上下文感知检索**（Contextual Retrieval）：
Anthropic 提出的技术：在向量化索引前用 LLM 为每个文本块生成含核心背景的前缀摘要并拼接后再索引，把孤立块重新锚定语义环境，同时增强稀疏与稠密检索。
_避免_：不要与第二章'上下文感知压缩'混淆——本技术在索引期对知识库文本块做加法补背景，压缩在运行期对会话历史做减法去冗余

**RAPTOR**（Recursive Abstractive Processing for Tree-Organized Retrieval）：
自下而上递归抽象的结构化索引：将文档切成叶子节点、聚类语义相近节点、用 LLM 为每组生成父节点摘要、递归形成从细节(叶)到概括(根)的知识树，支持多抽象层次检索。
_避免_：不要与 GraphRAG 混——RAPTOR 是树状层次(擅长概念→细节钻取)，GraphRAG 是实体关系图(擅长A与B关系/多跳)

**GraphRAG**（Graph-based RAG）：
将文档知识建模为实体-关系-实体三元组构成的知识图谱，核心优势是多跳关系推理(沿关系边遍历)和实体消歧(同名不同节点原生区分)，辅以社区发现生成主题摘要。
_避免_：不要与词义消歧混——GraphRAG 做的是实体消歧(区分两个同名张医生)，词义消歧(bank=河岸/银行)靠上下文感知嵌入即可

**智能体化 RAG**（Agentic RAG）：
把知识库检索封装成 Agent 可随时调用的工具，用 ReAct(思考→行动→观察)循环主导多轮迭代检索：自主决定关键词、评估信息是否充分、不够则提炼查询再搜，直到充分才作答。
_避免_：不要与非智能体化 RAG(固定单向 检索→生成 管道)混——后者被动、能力上限低，无法分解问题与迭代探索


## 支柱二 · 工具与 Coding Agent（第 4–5 章）

**ACI(Agent-Computer Interface)**（Agent-Computer Interface）：
对标HCI(人机交互)提出的概念:研究Agent如何与计算机交互，核心是让工具对Agent而非对人友好——工具应对应Agent的目标而非底层API操作。
_避免_：不要等同于'API'或'函数调用规范';ACI是设计理念层面的，不是某种具体协议。

**MCP(Model Context Protocol)**（Model Context Protocol）：
Anthropic于2024年底发布的开放标准，用客户端-服务器架构统一AI模型与外部工具、数据源的通信;定义工具、资源、提示三类原语，本地用stdio、远程用Streamable HTTP传输。
_避免_：不要说成'Anthropic的私有API';它是开放标准，被多家框架/IDE采纳。也别把它当成能解决异步唤醒的协议。

**参数传递的保真性**（Parameter fidelity）：
模型感知到的世界与工具操作的世界之间不得存在系统性偏差;工具不得在模型不知情时静默转换输入(如弯引号→直引号)或静默注入参数。
_避免_：不要窄化为'参数校验';它强调的是透明性——即使做规范化也必须在描述和返回中告知模型。

**提议者-审核者范式**（Proposer-Reviewer）：
用独立第二视角检验第一视角产出:一个模型提议行动(Proposer)，另一个能力相近但训练偏好不同的模型审查(Reviewer);含事前审批与事后验证(模态切换)两种机制。
_避免_：别理解成'同一模型自我反思';关键在认知多样性，两模型须来自不同家族。

**Sidecar 机制**（Sidecar）：
借鉴微服务边车模式的轻量LLM，与主模型流式输出并行运行，只看结构化工具调用数据(工具名+参数)做安全门控;对被审查的那次调用起门控作用，危险操作放行前不执行。
_避免_：不要说'Sidecar异步执行所以不阻塞审查'——它省的是排队时间，审查这道闸门仍在;也别说它读主模型的自由文本(恰恰刻意隔离)。

**事件驱动的异步Agent架构**（Event-driven async agent）：
将所有输入(用户消息、工具返回、外部回调、定时触发)统一建模为结构化事件流，用事件队列与事件循环驱动Agent思考，而非轮询检查新消息。
_避免_：不要与'多线程'或'并发编程'混为一谈;核心是事件建模与'训练同步/部署异步'矛盾。

**幂等性**（Idempotency）：
同一操作执行一次和多次对外部世界影响完全相同，因而可安全重试;手段有唯一标识去重、先查询后变更。不可幂等操作(发邮件/拨电话/转账)则用'预检-确认'两段式。
_避免_：别把所有操作都当成可幂等;发邮件、转账这类产生不可撤销真实事件的操作无法靠唯一标识去重。

**致命三要素**（Lethal Trifecta）：
Simon Willison提出的安全框架:访问私有数据、暴露于不可信内容、对外通信能力——三者齐备即构成完整攻击闭环;接入的MCP服务器越多越易集齐三要素(第五章系统展开)。
_避免_：本章只是引出，别把它讲成本章原创或已在本章展开;完整机制在第五章。

**元能力**（meta-capability）：
一种'能创造其他能力'的能力:Agent 用它当场写出新工具、新约束、新表达形式来完成任务,而不必事先把所有能力都预制好。代码生成正是这样的元能力。
_避免_：不要等同于'一个很强的能力'或'万能工具';关键在'创造能力的能力'而非能力本身强弱。

**Sessionless 设计**（Sessionless）：
OpenClaw 采用的无会话交互形态:没有安装/登录/打开App,Agent 常驻在线,用户通过已在用的消息平台随时发消息即得响应;其工程难点是代码执行环境与文件系统状态如何跨消息存活。
_避免_：不要理解为'无状态(stateless)';恰恰相反,它的核心挑战正是让状态跨消息、跨天持久存活。

**推测性执行(让安全检查隐形)**（speculative execution (UI)）：
把'展示'与'放行'并行:界面先显示无副作用的进度提示,后台同时跑安全检查,多数情况检查在用户察觉前完成。它不同于 CPU 推测执行——先行的只是无副作用UI提示,检查未过无需回滚,只是替换为'等待确认'。
_避免_：不要与 CPU 的推测执行画等号;这里没有需要丢弃/回滚的已算结果。

**权限内嵌的数据对象**（Permission-Embedded Data Objects）：
把数据不变量的强制执行从应用层下沉到其下:每个数据实体在一份人类审查过的 schema 里自带声明式权限规则、校验器和后果声明,一条三级运行时流水线在每次写入时强制执行,配合挂在每个操作上的'访问上下文'。
_避免_：不要与普通的应用层校验或 linter/contract 监控混淆;区别在于'哪一层被信任'被下移到数据层。

**字符串首尾匹配**（Old String Start + End → New String）：
文件编辑方案:模型只提供要删除内容的开头几行和结尾几行,中间省略,框架靠这对唯一的首尾组合定位替换区域。综合了文本替换的可靠性与行号方案的效率。
_避免_：不要与 Claude Code 的'旧字符串到新字符串'完全等同;它是后者处理大段改动的改进版,无需输出完整原文。

**委托方忠诚**（principal loyalty）：
多方委托处境下 Agent 到底站在谁那一边的问题:Agent 代表主人(principal)行事却要与利益相反的第三方打交道,存在'太老实(泄密/被施压让步)'与'太多疑(过度拒绝)'的两极翻车,二者是一根跷跷板。
_避免_：不要简化为'Agent 要忠诚';重点是两端都会翻车、且很难两全的忠诚度光谱。


## 支柱三 · 评估与后训练（第 6–7 章）

**模型替换实验**（Model Swap）：
固定 Harness、只更换更强或更弱的模型，观察分数变化幅度，用以区分性能瓶颈在模型能力还是在 Harness 设计。
_避免_：不要与消融实验混用——消融是关闭Harness组件，模型替换是换模型

**渐进式信息透露**（Progressive Information Disclosure）：
人机交互型评估的核心原则：不一开始把模拟用户的全部信息暴露给 Agent，而按需、渐进地在对话中透露，考察 Agent 主动澄清需求的能力。
_避免_：不是简单的多轮对话，关键在信息按脚本受控释放

**双控环境**（Dual-Control）：
τ²-bench 引入的机制：不再只有 Agent 一方能调用工具，用户模拟器也能操作同一共享环境（如用户切换飞行模式真正改变环境状态），更贴近技术支持等需用户动手配合的场景。

**数据泄漏**（Data Contamination）：
评估数据被纳入模型训练数据的现象，导致评估测的是记忆力而非泛化能力；防范手段含答案独特性、动态参数生成、时间新鲜度、canary GUID 等。
_避免_：不要简称为「过拟合」，泄漏特指评估题进了训练集

**一票否决机制**（Veto）：
Rubric 中的否决项设计：某维度（如幻觉、严重安全违规）一旦触发，无论其他维度多优秀都直接把总分判零，因为该维度与质量正交且伤害极大。
_避免_：区别于普通的分级评分维度，否决项不参与加权

**Pass^k**（Pass^k (Pass hat k)）：
k 次尝试全部成功的概率，回答「Agent 是否稳定可靠」，用于回归测试等看重稳定性的场景。
_避免_：切勿与 Pass@k（至少一次成功）混淆，二者衡量方向相反

**评判者校准**（Judge Calibration）：
在大规模使用 LLM 评判前，先在人工标注金标集(如100-200例)上测量评判模型与人类的一致率(简单一致率或 Cohen's kappa)，达门槛(如kappa>0.7)后才放量，且每次Rubric更新需重新校准。

**古德哈特定律**（Goodhart's Law）：
当一个度量指标变成优化目标时，它就不再是好的度量指标；Agent 越在某评分系统上调优，越倾向钻系统漏洞而非真正提升能力。

**位置偏差**（Position Bias）：
LLM 做配对评判时系统性偏向出现在某个位置(通常先出现)的候选，即使内容对调判决也可能不变；标准缓解方法是交换顺序各评一次取平均。
_避免_：区别于长度偏差(偏向更长回答)

**监督微调**（SFT (Supervised Fine-Tuning)）：
用几千到几万条'问题—标准回答'示范对继续做'预测下一个词'、且只在回答 token 上算损失的后训练方法，把指令遵循、格式、风格、流程等协议性知识固化进参数。
_避免_：不要泛称为'微调'或等同于'训练'；它特指有标注输入-输出对、带 loss masking 的方法。

**损失屏蔽**（loss masking）：
计算 SFT/工具 RL 损失时把不希望模型学习的 token（问题部分、环境返回的反馈 token）屏蔽掉，只对模型自己生成的回答 token 回传梯度。
_避免_：不要与注意力掩码(attention mask)混为一谈，两者作用位置不同。

**反向 KL 散度**（reverse KL）：
RLHF 中写成 KL(π_θ‖π_ref)、当前策略在前的 KL 惩罚方向，惩罚'当前策略给高概率而参考模型几乎为零'的情况，把模型摁在参考模型的安全区附近，是 mode-seeking 的。
_避免_：不要和正向 KL(KL(π_ref‖π_θ)、mass-covering)混用；方向不是随便写的。

**寻峰与覆盖式**（mode-seeking vs mass-covering）：
两种概率分布拟合性格：极大似然(SFT)是 mass-covering，努力覆盖示范里所有模式、雨露均沾；反向 KL 的策略优化(RL)是 mode-seeking，只集中到少数几个高奖励峰、赢者通吃。
_避免_：不要把 mode-seeking 简单等同于'过拟合'，它是分布拟合方向的固有性质。

**可验证奖励强化学习**（RLVR (RL with Verifiable Rewards)）：
奖励来自规则验证器(测试是否通过、答案是否正确)而非学习到的奖励模型的 RL；此时'奖励模型'退化成一段确定性代码。
_避免_：不要与 RLHF(奖励来自人类偏好训练的 RM)混淆，也不要与只差一字母的 RLVP 混淆。

**验证路径惩罚**（RLVP (RL with Verified Penalty)）：
在结果奖励 O 之外加一路确定性规则引擎给出的路径信号 Φ(R=O+β·Φ)：违规动作扣分、合规/可达进展动作加分——'奖励结果、惩罚路径'。
_避免_：不要与 RLVR 混淆:RLVR 验证结果,RLVP 额外验证过程,两者互补叠加。

**信用分配**（credit assignment）：
多步序列中判断到底哪一步对最终成败贡献最大的问题;由折扣因子、GAE(PPO)、组内相对比较(GRPO)、turn-level 分摊等机制承担。
_避免_：不要与'奖励设计'画等号;信用分配是奖励延迟下的归因难题。

**在轨蒸馏**（On-Policy Distillation）：
让学生自己生成轨迹(在轨),更强教师对每一步逐 token 给出目标概率分布(稠密),学生最小化与教师分布 KL 的方法;是'在轨+稠密信号'的组合。
_避免_：不要与普通(离轨)蒸馏或 CoT 蒸馏混淆——关键区别是轨迹由学生而非教师采样。


## 进阶应用 · 自我进化 / 多模态 / 多 Agent（第 8–10 章）

**外部化学习**（Externalized Learning）：
把知识沉淀到模型之外的文件、知识库和工具中的学习范式,持久、可解释、可随时修正;含'经验外部化为知识/Skill'与'能力外部化为工具'两个维度。
_避免_：不要等同于'记忆'或泛泛的'持久化';它特指不改权重的第三条学习路径

**策略摘要**（Strategy Summary）：
把一次成功解题过程浓缩为结构化经验笔记(方法、踩过的坑、关键步骤),向量化存入知识库供后续语义检索。
_避免_：不要与'上下文压缩'混同;策略摘要是跨会话可迁移的经验,不是单次会话的临时压缩

**睡眠整合**（Sleep-time Consolidation）：
经验积累与记忆整合分离,后台异步周期性运行独立子Agent执行定向→采集→整合→修剪四阶段,整理的主要是用户记忆。
_避免_：不要理解为整理共享知识库;Claude Code'睡眠'里整理的主要是用户记忆而非RAG领域文档

**系统提示学习**（System Prompt Learning）：
Karpathy提出的学习方式,直接修改系统提示词文本从边界案例快速学习明文规则,不改模型参数。
_避免_：不要与微调/RL混为一谈;它不做梯度下降,学习算法是改文本

**主动工具发现**（Active Tool Discovery）：
Agent执行中意识到能力缺口时用自然语言声明需求,系统按服务器级→工具级两层语义路由动态匹配并注入工具schema。
_避免_：区别于第四章的'一次性检索预筛选';主动发现是执行过程中随需要多次触发

**最小预定义、最大自我进化**（Minimal Predefinition, Maximal Self-Evolution）：
Alita论文提出的设计原则:只给少量基础工具提供与世界交互的基本能力,自我进化机制在此之上赋予无限扩展潜力。
_避免_：不要理解为'不要任何预定义工具';是'最小'而非'零'

**记忆与经验投毒**（Memory Poisoning）：
Agent接触的网页文本/工具输出中的恶意注入指令若未经审查被沉淀为长期记忆或Skill,攻击从一次性变为跨会话持久生效。
_避免_：区别于会话内的一次性提示注入;投毒污染的是Agent的'知识'本身,更隐蔽难清除

**级联流水线**（Cascaded Pipeline）：
把 VAD、ASR、LLM、TTS 四个独立模型串成一条接力式流水线的语音架构，模块清晰可独立调优，但延迟逐段累积。
_避免_：不要与「自级联 self-cascade」混淆——后者是同一模型先转录再推理

**端到端全模态模型**（Omni）：
用单一模型直接听音频、想回复、说出来，把 ASR-LLM-TTS 三段合一，延迟低且能保留韵律情感，但仍假设轮流说话、依赖 VAD 划分轮次。
_避免_：不等于全双工；OpenAI 称其为 turn-based，与工业界的 Omni 是同一类东西

**全双工/交互模型**（Full-Duplex / Interaction Model）：
模型同时持续地听和说，以约 200ms 微轮次交织多路流、每秒多次决策该说该听该停该打断，彻底取消轮次假设（Moshi、GPT-Live、TML-Interaction）。
_避免_：不要与 Omni 的「支持打断」混为一谈——那仍在 VAD 框架内做优化

**结束点检测**（End-of-Speech Detection）：
VAD 的关键设计，通常用 500-800ms 连续静音阈值判断用户是否说完，阈值太短截断句子、太长干等，两难。
_避免_：不要简称为「静音检测」而忽略它其实要区分轮内停顿与轮间间隔

**文本代理思考**（Textual Surrogate Reasoning）：
音频模型「思考」时实际基于文本转录做语义分析、而非真正分析声学特征的捷径现象，导致思考链越长性能反而越差。
_避免_：不是指思维链本身有问题，而是指思考根基错位到了文本而非声音

**模态锚定思考蒸馏**（MGRD (Modality-Grounded Reasoning Distillation)）：
Step-Audio R1 的机制：筛选出真正基于声学特征（如语速、音高轮廓）而非歌词/文字的思考过程来迭代训练，让模型「用耳朵思考」。
_避免_：不要与 MPS 双脑架构混淆——MGRD 解决「想得对不对」，MPS 解决「说得及不及时」

**MPS 双脑架构**（Mind-Paced Speaking）：
Step-Audio R1 用构思脑（持续思考产出分段结果）与表达脑（每收到一段就转成语音）两模型并行，实现低延迟的边想边说。
_避免_：这是「思考/表达」的解耦，不同于机器人双层架构「谋划/执行」的解耦

**动作分块**（Action Chunking）：
VLA 模型一次推理生成未来一小段动作序列（如 π₀ 在 50Hz 下 25-50 个动作）、控制线程高频回放的频率补偿技术，最早由 ACT 提出。
_避免_：不是简单批处理；核心是拿反应性换平滑性，块越长对突发变化越迟钝

**共享上下文 / 不共享上下文**（Shared / Non-shared Context）：
多 Agent 的第一设计维度：共享指后一 Agent 继承前一 Agent 的完整轨迹（隐式协调、信息零损耗、上下文膨胀）；不共享指各 Agent 维护独立上下文，仅通过工具参数/共享文件/消息总线三种显式机制通信。
_避免_：不要与「多轮对话历史」混同——这里区分的是 Agent 之间是否继承彼此的完整轨迹

**移交（handoff / 移交包）**（Handoff）：
由上游 Agent 主动决定把哪些信息交接给下游。不共享上下文下的「移交包」通常含任务描述、已确认的事实与约束、结构化产物的引用（文件路径而非内容），刻意不传全量轨迹。
_避免_：不要理解成「把完整历史一并交出」——那是共享上下文下的形态

**消息总线**（Message Bus）：
专门在 Agent 间中转消息的基础设施，Agent 发布/订阅消息而不直接调用彼此，天然支持异步、多方并行；常见实现有 Redis Pub/Sub（不持久化）与 RabbitMQ（持久化）。
_避免_：不要与点对点通信混用——总线的意义正是解耦收发双方、避免连接数平方增长

**乐观锁**（Optimistic Locking）：
共享文件系统的并发控制策略：每个文件维护版本号，Agent 读时记录版本、写时检查是否仍一致，不一致则写入失败并被迫基于最新版本重做。只能防同一文件的写入冲突。
_避免_：不要与悲观锁混同（后者打开即锁定）；也不要指望它能解决跨文件语义冲突

**级联终止**（Cascading Termination）：
并行协作中「一者成功、余者失效」的机制：某 Agent 命中目标后，Manager 立即向其余仍运行的 Agent 广播 terminate，各自优雅停止并回 ack；需用锁/幂等设计处理多个 Agent 近乎同时上报成功的竞态。
_避免_：不要忽略竞态条件——不做锁保护会导致重复结算或状态矛盾

**A2A 协议**（Agent2Agent）：
2025 年 Google 发布（后捐给 Linux 基金会）的跨组织 Agent 互操作协议，三要素为 Agent Card（能力名片）、任务生命周期管理（带状态机的 Task）、不透明协作（只交换任务与 Artifact，不暴露内部实现）。
_避免_：不要与 MCP 混同——MCP 解决 Agent 与工具互操作，A2A 解决 Agent 与 Agent 互操作

**涌现行为**（Emergent Behavior）：
系统整体表现出的、无法从单个个体行为规则直接预测的集体行为模式（如蚁群找最短路径、AI 小镇自发组织派对）；当 Agent 数量跨过临界点、交互足够自由时出现。
_避免_：不要与「预先编排的协作」混同——涌现的关键是没有显式的组织代码

**MAST 失败模式分类法**（MAST）：
2025 年论文《Why Do Multi-Agent LLM Systems Fail?》提出的分类法，在 7 个主流框架上归纳出 14 种失败模式，分三大类：系统设计缺陷、Agent 间对齐失败、任务验证缺失。
_避免_：不要把它当成可靠工程 bug 清单——论文认为这些是根本性设计缺陷，简单修补收益有限

